name: Tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install dependencies
      run: sudo apt-get install libevent-dev
    - name: Build lib/
      run: mkdir build && cd build/ && cmake .. && make

  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Test lib/
      run: mkdir build && cd build/ && cmake .. && make test
